server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Akankshya1402/flight-config-repo
          default-label: main
          clone-on-start: true
          force-pull: true
          timeout: 10
